---
title: '<분야> - <무엇을 다루는가> (feat. <핵심 키워드 1~2개>)'

date: YYYY-MM-DD HH:MM:SS +0900   # 최초 게시 시각. 발행 직전에 실제 시각으로 채운다

categories: ["<slug>"]            # machine-learning / deep-learning / nlp-llm / optimization
                                  # paper-review / mlops / python-data / retrospective
tags:
  - "<세부 주제>"
  - "<개념정리 | 구현실습 | 트러블슈팅 | 논문리뷰>"

use_math: true

toc: true
toc_label: "목차"
toc_icon: "list"

# 공유 미리보기 이미지. 글의 대표 그림을 지정한다. 없으면 사이트 기본값이 쓰인다.
header:
  og_image: /assets/images/YYYY-MM-DD-<slug>/01-....png
---
<!--
  ── 도입부 규칙 (헤딩 없이 4~8줄) ────────────────────────────────
  아래 네 가지를 반드시 이 순서로 밝히고 시작한다. 바로 본론으로 들어가지 않는다.

  1. 배경   : 어떤 상황에서 이 문제를 만났는가. 무엇을 하다가 나온 이야기인가
  2. 계기   : 왜 이걸 파고들게 됐는가. 처음에 뭘 오해했는가
  3. 질문   : 이 글이 답하려는 질문 한 문장 (글의 축)
  4. 재현성 : 측정으로 확인했다는 것과 스크립트 링크

  회사/업무 소재라면 그대로 옮기지 말고, 같은 현상을 재현하는
  장난감 문제를 새로 만들어 직접 돌린 결과로 쓴다.
  실명과 조직명, 내부 식별자, 내부 파일 경로는 절대 넣지 않는다.
-->

<배경 1~2줄>  
<계기 1~2줄. 처음의 오해를 솔직히>  

**<이 글이 답하려는 질문 한 문장>**  

아래 내용은 전부 직접 돌려본 결과이고, [스크립트](/assets/code/<name>.py)로 재현할 수 있다.  

## <문제 설정>

### <장난감으로 줄이기>

<!-- 용어와 기호는 처음 쓰기 전에 정의한다. 독자가 표를 읽을 수 있어야 한다 -->

<br>
<br>
<br>

## <실험>

### <실험 1>

![<이미지를 설명하는 alt. 비워두지 않는다>](/assets/images/YYYY-MM-DD-<slug>/01-....png)

<이미지 바로 다음 줄에 해설을 붙인다>  

## 정리

* <결론 3~5개. 본문 문장을 그대로 반복하지 말고 판단 기준으로 쓴다>  

아직 못 본 것도 적어둔다.  
<한계, 미검증 항목, 다음에 볼 것>  

<!--
  ── 문체 규칙 ───────────────────────────────────────────────────
  1. 평서체 '~다'로 쓴다. 한 문장을 한 줄에 두고 줄 끝에 공백 2칸을 둔다
  2. 실패한 과정을 지우지 않는다. 처음의 오해를 그대로 남긴다
  3. 가운뎃점 (U+00B7) 은 어디에도 쓰지 않는다
       같은 급 나열은 쉼표, 이질적 항목 구분은 파이프(|)
  4. 일본식 낫표 (U+300C, U+300D) 와 겹낫표 (U+300E, U+300F) 는 어디에도 쓰지 않는다
       인용과 강조는 큰따옴표, 코드와 식별자는 백틱
  5. 한 문장 안에서 '~가 ~해서 ~함'으로 원인과 판단, 결과를 잇지 않는다
  6. 볼드는 본문 8개 안팎. 핵심 결론에만 쓴다

  ── 발행 전 점검 (문체는 눈으로 보지 말고 반드시 돌린다) ────────
  □ python3 scripts/check_style.py _posts/<파일>.md  가 위반 0으로 끝났는가
       이 검사를 통과하지 않은 글은 커밋하지 않는다
  □ 도입부에 배경과 계기, 질문, 재현성이 다 있는가
  □ date 가 '현재보다 과거'인가 (미래 시각이면 Jekyll 이 조용히 발행을 건너뛴다)
  □ 스크립트가 실제로 돌아가고 글의 수치를 재현하는가
  □ 발행 뒤 실제 URL 을 열어 200 과 렌더링을 확인했는가
-->
